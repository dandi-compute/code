# How to run (on Engaging)

To run from working directory `pipeline-aind+ephys/dandiset-[dandiset id]/sub-[subject id]/ses-[session id]/run-[run id]/`

```bash
bash ../../../../submit.sh [blob ID] [run ID]
```
